# かじた式スロット 引き継ぎ書

最終更新：2026-06-20 / バージョン：v7

## 1. 概要
3つの数字を回して「いい数字」を引き当てる、語呂合わせ占いスロット。
全1000通り（000〜999）すべてに語呂合わせ＋ポジティブなコメントが付く。
当たりは **レア / スーパーレア** の2段階で派手に演出される。

- **公開URL**：https://sin1.studio/lucky-number-slot/
- **リポジトリ**：https://github.com/sin1n24/lucky-number-slot （public）
- **配信**：GitHub Pages（main ブランチのルート）。独自ドメイン sin1.studio 配下に出る
- **着想元**：わしゃがなTV「マフィア梶田式スロット」（公開版は名称を「かじた式」に変更）

## 2. ファイル構成
| ファイル | 役割 | Git管理 |
|---|---|---|
| `index.html` | 本体（HTML/CSS/JS 全部入りの単一ファイル） | ◯ |
| `ogp.png` | OGP画像 1200×630 | ◯ |
| `README.md` | リポジトリ説明 | ◯ |
| `HANDOVER.md` | この引き継ぎ書 | ◯ |
| `.gitignore` | 除外設定 | ◯ |
| `ogp_gen.html` | OGP画像を作るための元HTML（作業用） | ✗（gitignore） |
| `verify*.png` / `.playwright-mcp/` / `srv.log` | 検証作業の生成物 | ✗（gitignore） |

すべて `index.html` 1枚で動く。ビルド工程・依存ライブラリなし。外部CDNも未使用（GAタグのみ動的読み込み）。

## 3. 仕組み（言葉の決まり方）
`index.html` 内のJSで、3桁の数字 → 言葉＋コメント を決める。優先順位は次の通り。

1. **`DICT_NUM`（番号直結辞書）** … `'040':['お塩','…']` のように **番号で直接**指定。最優先。
2. **`DICT`（読み辞書）** … かなの読みで引く。`KANA`/`ALT` で数字→かなに変換し、別読みの組み合わせ（`cartesian`）で一致を探す。
3. **フォールバック** … 上で当たらない番号は、読みの音から `THEME` を使って短い前向き文を自動生成（必ず何か出る）。

### 主要データ（index.html 内）
- `KANA` … 各数字の基本読み（0→お,1→い,2→に,3→さ,4→し,5→こ,6→む,7→な,8→は,9→く）。例 014→おいし、087→おはな。
- `ALT` … 数字ごとの別読み候補（辞書マッチ用）。
- `DICT` … 読み（かな）→[表示語, コメント]。
- `DICT_NUM` … 番号→[表示語, コメント]。**追加・修正は基本ここ**。
- `SUPER` … スーパーレアの番号集合（Set）。ここに入れた番号が「スーパーレア」になる。
- `RARE` … ゾロ目・定番の派手バナー専用メッセージ（番号→文）。

### ティア判定 `rarityOf(combo)`
- `SUPER` に含まれる → **'super'（スーパーレア）**
- それ以外で言葉が当たる（DICT_NUM/DICT ヒット、または RARE 登録） → **'rare'（レア）**
- どちらでもない（フォールバックのみ） → null（通常）

現在の数：スーパーレア **50通り** / レア **237通り**（残りは通常）。

## 4. よくある編集
### 言葉を足す／直す
`DICT_NUM` に1行追加するだけ。
```js
'235':['にさみ','…ここにポジティブなコメント…'],
```
### スーパーレアにする
`SUPER` の `new Set([...])` に番号を足す。
```js
'235',
```
### バナーの派手文言を変える（ゾロ目・定番）
`RARE` を編集。`SUPER` の番号で `RARE` が無いものは、自動で「(言葉)！誰もが納得の超しっくり当て字」と表示。

## 5. 演出まわり
- **スロット**：`reels`（3リール）。タップ or 「ストップ」ボタンで停止。最後の1つはレアで遅く、スーパーレアで最も遅く止まる。
- **音**：WebAudioで生成（`playSelect/playStop/playDecide/playRare/playSuper/playTick`）。外部音源なし。
- **記録（草タイル）**：1000ドット。出た番号を緑、レアをピンク、スーパーレアを金で記録。
  - 保存先：`localStorage` キー **`mks_seen_v1`**。「記録をリセット」で消える。
  - カウンタ：`出た / 1000`、`スーパーレア (出 / 50)`、`レア (出 / 237)`。

## 6. 先頭の差し替え用定数
`index.html` の上部にある。
- `<script>` 内 `GA_ID` … Googleアナリティクス GA4 測定ID（現在 `G-6S14NKHBKX`）。`"G-XXXXXXXXXX"` のままなら読み込まれない（無効）。
- JS下部付近 `STUDIO_URL`（https://sin1.studio）, `VIDEO_URL`（わしゃがなTVの該当動画）。
- クレジットの `v7` 表記は `<span class="ver">` を手で更新。

## 7. Googleアナリティクス（GA4）
- `<head>` に GA4 タグを設置。`GA_ID` を実IDにすると有効化。プレースホルダのままなら何も読み込まない（エラーも出ない）。
- 計測確認：GA管理画面 → レポート → リアルタイム。広告ブロッカーで弾かれることがあるのでシークレット窓推奨。

## 8. OGP（Twitterカード）
- `<head>` の `og:*` / `twitter:*` メタで設定。画像は `https://sin1.studio/lucky-number-slot/ogp.png`。
- **画像の作り直し方**：`ogp_gen.html`（1200×630のカード）をブラウザで開き、ビューポート1200×630でスクショ→`ogp.png` として保存→コミット。
- X/LINEはOGPを数日キャッシュする。更新直後は [Card Validator](https://cards-dev.twitter.com/validator) でキャッシュ更新可能。

## 9. デプロイ手順
mainにpushすればGitHub Pagesが自動で再ビルド（1〜2分）。
```
cd "C:\Users\sin1n\MafiaKajitaSlot"
git add -A
git commit -m "変更内容"
git push
```
ビルド状態：`gh api repos/sin1n24/lucky-number-slot/pages/builds/latest --jq .status`（`built` で完了）。

## 10. バージョン履歴
- v1 初版（スロット・語呂合わせ・1000ドット・シェア・レア）
- v2-3 ドラッグ選択防止、ボタン統合、タップ演出、シェア堅牢化、フォールバック短縮、語呂追加
- v4 番号直結辞書 DICT_NUM 導入、定型文をレア化、レア総数表示
- v5 レア/スーパーレアの2段階化、SUPER集合、金ドット、超ファンファーレ
- v6 名称を「かじた式」に変更、GA4タグ設置、GitHub Pages公開
- v7 シェアをTwitter前提、★削除、シェアボタンを青(X)系、OGP対応＋OGP画像

## 11. 注意点・既知の制約
- ローカル確認は `file://` 不可（fetch等の都合）。簡易サーバ（例 `python -m http.server`）経由で開く。
- 記録はブラウザの localStorage 依存（端末・ブラウザごと。サーバ保存はしていない）。
- フォントは端末標準（Yu Gothic / Hiragino 等）。環境で見た目が多少変わる。

---
製作：sin1's studio（https://sin1.studio）／ 着想元：わしゃがなTV
